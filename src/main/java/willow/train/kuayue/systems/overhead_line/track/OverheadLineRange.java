package willow.train.kuayue.systems.overhead_line.track;

import kasuga.lib.core.create.boundary.CustomTrackSegment;

import java.util.UUID;

public class OverheadLineRange extends CustomTrackSegment {
    public OverheadLineRange(UUID segmentId) {
        super(segmentId);
    }
}
